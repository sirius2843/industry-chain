# 2026-09-11 工作日志

## 16:00 产业链看板每日刷新（定时任务）
- 运行 `bash scripts/daily_refresh.sh`，退出码 0，成功推送（289fc4f..207f029 main -> main）
- 四条链最新交易日均为 2026-09-11；序列覆盖：AI 663、医药 452、机器人 224、有色 149
- 龙头变化：仅 AI 链「智能终端」环节回摆 —— 传音控股(688036.SH) -> 华勤技术(603296.SH)，佐证为华勤 +2.17%/流通市值 613 亿 反超 传音 +0.76%/607 亿
- 医药/机器人/有色各环节龙头均无变化
- AI 链 1 只缺 daily：839493.BJ（北交所，历史一致，非异常）
- 通联 token 正常，无 401；Python 依赖正常；工作区干净，HEAD=origin/main=207f029

### 复用要点
- 对比各环节龙头的可靠方法：解析 HTML 中 `"segments": [...]` 数组（用括号配平截取后 json.loads），取 `cos` 内 `l==1` 的成员；不要用正则直接匹配成员对象（字段顺序不统一，`"l": 1` 冒号后带空格）
- 个股当日涨跌幅可从 `data/dash_wm/px_*.csv` 计算（长表格式：ticker/tradeDate/closePrice/negMarketValue），ticker 无交易所后缀
